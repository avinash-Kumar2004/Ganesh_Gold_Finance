import { useLanguage } from "../Common/Navbaar";
import Slider from "../Common/Slider";
const Policy = () => {
  const { t } = useLanguage();
  return (
   <>
   <Slider/>
   </>
  );
};

export default Policy;